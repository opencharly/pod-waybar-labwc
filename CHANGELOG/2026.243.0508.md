# 2026.243.0508 — chore: advance the pod-labwc pin, and the charly pin it now requires

## Summary

This candy's pinned refs were behind their repos' newest tags. That matters beyond tidiness:
**a published tag embeds the pins it carried at tag time**, so every consumer that pins a tag
of this candy drags those old refs into its own resolution, and charly reports them as
`resolved to multiple versions` on every build of that consumer.

The pins advanced are listed in the commit message.

**Which `version:` moves, precisely.** `charly.yml` carries two: the TOP-LEVEL `version:` is
the SCHEMA STAMP (e.g. `2026.232.0520`), which identifies the config schema the file is
written against and is deliberately **unchanged** — bumping it would claim a schema the file
does not use and would be rejected by the repo's pinned charly. What moves is the candy's OWN
CalVer, nested under the candy node (`<name>.candy.version`), because changed content must
ship under a new version rather than reusing one already released. The diff shows exactly
that: the nested `version:` advances, the schema stamp stays put.

### Why this is being done across the org

A downstream project (`distro-cachyos`) could not reach a zero-warning build, and the cause
was traced to this class of staleness rather than to anything in that project. Measured
there, advancing a *consumer's* pin makes it worse, not better:

| Change in distro-cachyos | Warnings |
|---|---|
| baseline | **2** |
| advance one consumer | 3-6 |
| advance ALL 23 stale pins (of 177) | 8 |

Advancing a consumer to a newer tag simply exposes that tag's *other* stale refs. The only
operation that removes an edge rather than trading it for others is fixing at the source, so
the next tag cut here carries current pins throughout.

A full scan of the 178-repo closure found **93 repos carrying 188 stale pins, with zero
dependency cycles** - so the graph can be repaired in dependency order, and this repo is part
of that ordered sweep. Analysis, measurements and options: opencharly/distro-cachyos#48.

## How tested

```
$ charly box validate
exit 0
```

Run with the charly version this repo's own `deploy.yml` clones and builds, so it is the same
gate CI runs rather than a newer local binary - a mismatched binary rejects the repo's schema
stamp outright. The exit code was read directly from `charly box validate`, not through a
pipeline.

The diff touches TWO files here, and the second is the point: `charly.yml` (the ref pin plus
the candy's nested CalVer) **and** `.github/workflows/deploy.yml` (the charly version it
clones). Advancing the candy pin alone breaks the gate, because the newer `pod-labwc` authors
`distro:` in the MAP form and the previously-pinned charly cannot parse it:

```
candy "github.com/opencharly/pod-labwc": CUE schema: candy "labwc":
  distro: conflicting values {arch:{package:[...]},fedora:{package:[...]}}
  and [...(string & !="")] (mismatched types struct and list)
```

Three binaries were measured to find the right one rather than assuming newest is best:

```
charly v2026.238.1242 (pinned before)   exit 1   schema not understood
charly v2026.240.2035                   exit 0
charly v2026.241.1130                   exit 1   stamp mismatch
```

So `v2026.240.2035` is the minimal charly accepting both this repo's schema stamp and the
newer `pod-labwc`. No packages, services or plan steps change, and no image is built here. The consuming project's warning drop lands
with the repins that follow these tags.

## Rulebook compliance

- **R0 (skills)** - `/charly-image:layer` for the candy/ref model;
  `/charly-internals:git-workflow` for PR-only landing and tag-on-merge.
- **R1 (RCA)** - the mechanism is identified precisely (published tags embed pins at tag time)
  and the consumer-side alternative was measured to be worse, not assumed.
- **R2 (no deferral)** - a stale-pin set found while chasing a downstream warning is fixed in
  the repo that owns it, not labelled pre-existing.
- **R3 (no duplication)** - pins advanced in place; nothing restated locally.
- **R4 (no workarounds)** - pins are advanced rather than the warnings allowlisted. Silencing
  a true signal to pass a gate was considered and rejected.
- **R5 (hard cutover)** - the pin values change with no old path left behind.
- **R6 (git safety)** - feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** - `charly box validate` run on the committed tree with the
  repo's own pinned charly.
- **R8 (generation)** - N/A: no generated artifact, no CUE input in this repo.
- **R9 (artifact integrity)** - N/A: no image is built here.
- **ADE (candy description/plan)** - N/A: `description:` and `plan:` untouched, so nothing
  re-projects.
- **SDD (CUE source)** - N/A: no schema authored or generated here.

## Change classification

- **Change class:** candy ref pin bumps + the candy's own CalVer bump. Config only.
- **Verification gate:** `charly box validate` under the repo's pinned charly, exit 0
- **Attribution tier:** `analysed on a live system` - the gate was executed on a real checkout
  with the exit code recorded, and the skew this removes was counted across real bed builds;
  no image is built in this repo

*Assisted-by: Claude Code Anthropic Opus 5 (analysed on a live system)*